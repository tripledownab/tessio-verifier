{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Tessio.Verifier": "Debug"
    }
  },
  "AllowedHosts": "*",

  "// PublicBaseUri": "Where the suite reaches us. It runs in Docker, so host.docker.internal, not localhost.",
  "PublicBaseUri": "http://host.docker.internal:5099",

  "// Suite": "Both values come from the test plan you create in the suite. Put them in appsettings.Local.json.",
  "Suite": {
    "AuthorizationEndpoint": "",
    "Issuer": ""
  },

  "// Request": "Must match the variants selected for the test plan, or the module fails for the wrong reason.",
  "Request": {
    "Claim": "age_over_18",
    "CredentialFormat": "dc+sd-jwt",
    "ResponseMode": "DirectPostJwt",
    "ExpectedVct": "urn:eudi:pid:1"
  }
}
